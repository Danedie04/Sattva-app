name: sattva_app
description: Sattva - Culturally personalized health habit tracker

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  flutter_local_notifications: ^17.0.0
  percent_indicator: ^4.2.3

  # Firebase
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.2.1
  firebase_analytics: ^11.2.1

  # Premium UI
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0
  http: ^1.2.1

flutter:
  uses-material-design: true
